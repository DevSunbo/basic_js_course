const array = [1, 3, 5, 7, 9];
// console.log(array);

const objects = [{ name: "멍멍이" }, { name: "야옹이" }];
console.log(objects);

objects.push({ name: "짹짹이" }); // 가장 마지막에 들어간다
console.log(objects.length);
